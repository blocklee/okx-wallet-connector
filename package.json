{
  "name": "okx-wallet-connector",
  "version": "1.0.0",
  "description": "evm connector",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "dev": "npm run clean && tsc -p tsconfig.json --watch",
    "build": "npm run clean && tsc -p tsconfig.json"
  },
  "keywords": [
    "evm",
    "wallet",
    "connector"
  ],
  "author": "okfe-wjh",
  "license": "MIT",
  "dependencies": {
    "@wagmi/core": "0.10.8",
    "typescript": "^5.2.2"
  }
}
